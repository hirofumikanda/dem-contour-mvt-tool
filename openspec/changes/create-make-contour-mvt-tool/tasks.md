## 1. プロジェクト基盤整備 (Issue: #1)

- [x] 1.1 スクリプト置き場（例: `bin/` または `scripts/`）と中間生成物置き場（`build/`）のディレクトリ構成を決めて作成する
- [x] 1.2 依存CLIツール（gdalbuildvrt, gdal_contour, mapshaper, tippecanoe, tile-join, pmtiles, python3）の存在チェックを行う簡易スクリプトを用意する
- [x] 1.3 パイプライン全体を通しで実行するエントリーポイント（例: `make-contour-pmtiles.sh`）の骨格を作成し、各ステップを順に呼び出す構造にする

## 2. VRT統合ステップ (Issue: #2)

- [x] 2.1 `tif/`配下のGeoTIFF群を入力に`gdalbuildvrt`でVRTを生成する処理を実装する（NoData値 -9999 を `-srcnodata`/`-vrtnodata` で明示）
- [x] 2.2 生成したVRTに対し`gdalinfo`でNoData値・範囲が想定どおりであることを確認する検証コマンドを実装（またはテストに組み込む）

## 3. 等高線抽出ステップ (Issue: #3)

- [x] 3.1 VRTから`gdal_contour -f GeoJSONSeq -i 10 -a elevation`で10m間隔のndjsonを生成する処理を実装する
- [x] 3.2 同様に100m間隔・500m間隔のndjsonを生成する処理を実装する（`-i 100`, `-i 500`）
- [x] 3.3 生成された3つのndjsonの各行が有効なGeoJSON Featureとしてパースでき、標高値の属性を持つことを確認する

## 4. 簡略化・平滑化ステップ (Issue: #4)

- [ ] 4.1 mapshaperを用いて、100m間隔ndjsonに対しWeighted Visvalingam簡略化（`-simplify weighted ...`）を適用するラッパー処理を実装する
- [ ] 4.2 500m間隔ndjsonに対しても同様にmapshaperでの簡略化を適用する（100mより強めの簡略化パラメータを設定する）
- [ ] 4.3 Chaikinのコーナーカット平滑化を行うPythonスクリプトを実装する（ndjsonを1行ずつ読み、LineString/MultiLineStringの座標配列に指定反復回数のコーナーカットを適用し、始点・終点を保持して書き戻す）
- [ ] 4.4 簡略化（mapshaper）→平滑化（Chaikinスクリプト）の順で100m・500mのndjsonに適用するパイプライン処理を実装する
- [ ] 4.5 10m間隔のndjsonにはこのステップを適用しない（未加工のまま次段へ渡す）ことをスクリプト上で明示する
- [ ] 4.6 簡略化・平滑化前後で頂点数が減少していること、および形状が過度に歪んでいないこと（自己交差が発生していないこと）を確認する検証処理を実装する

## 5. タイル生成ステップ (Issue: #5)

- [ ] 5.1 10m間隔ndjson（未簡略化）からtippecanoeでz14のみのMBTilesを生成する処理を実装する
- [ ] 5.2 簡略化・平滑化済み100m間隔ndjsonからtippecanoeでz11-z13のMBTilesを生成する処理を実装する
- [ ] 5.3 簡略化・平滑化済み500m間隔ndjsonからtippecanoeでz7-z10のMBTilesを生成する処理を実装する
- [ ] 5.4 tippecanoeの内部簡略化が事前のChaikin平滑化結果を過度に劣化させないか確認し、必要に応じてtippecanoeオプション（簡略化抑制系フラグ）を調整する
- [ ] 5.5 生成した3つのMBTilesそれぞれのメタデータ（minzoom/maxzoom）が意図通りであることを確認する

## 6. MBTiles統合とPMTiles変換 (Issue: #6)

- [ ] 6.1 `tile-join`で10m用・100m用・500m用の3つのMBTilesを1つのMBTilesに統合する処理を実装する
- [ ] 6.2 統合MBTilesのメタデータのズーム範囲がz7-z14であることを確認する
- [ ] 6.3 `pmtiles convert`で統合MBTilesをPMTilesに変換する処理を実装する
- [ ] 6.4 生成したPMTilesを`pmtiles show`/`pmtiles verify`で検証し、破損なく読み込めることを確認する

## 7. パイプライン統合と実行 (Issue: #7)

- [ ] 7.1 1〜6の各ステップをエントリーポイントスクリプトから順に実行できるように結線する（各ステップの入出力ファイルパスを一貫させる）
- [ ] 7.2 主要パラメータ（等高線間隔、ズーム範囲、簡略化強度、Chaikin反復回数、入出力ディレクトリ）を引数または設定ファイルで指定できるようにする
- [ ] 7.3 `tif/`配下のサンプルGeoTIFF（2ファイル）を入力にエントリーポイントスクリプトを実行し、エラーなくPMTilesが生成されることを確認する

## 8. テスト (Issue: #8)

- [ ] 8.1 サンプルGeoTIFFを使ったエンドツーエンドテスト（VRT生成〜PMTiles生成まで一気通貫）を作成し、各中間生成物と最終PMTilesの存在・妥当性（ズーム範囲、タイル数、ジオメトリの妥当性など実際の内容）を検証する
- [ ] 8.2 簡略化・平滑化処理単体のテストを作成し、意図的に角張った入力ラインに対して頂点数が減ること・角度が緩和されること・始終点が保持されることを具体的な座標値で検証する（無意味な `true === true` 等のアサーションは書かない）
- [ ] 8.3 10m間隔のデータが簡略化・平滑化をスキップし元データと一致することを検証するテストを作成する
- [ ] 8.4 異常系（NoDataのみのGeoTIFF、範囲が重ならない複数GeoTIFF、空の等高線結果など）に対してパイプラインが適切にエラーまたは空成果物を扱うことを確認するテストを作成する

## 9. ドキュメント (Issue: #9)

- [ ] 9.1 パイプラインの使い方（前提ツール、実行コマンド、パラメータ、出力物の説明）をREADME等にまとめる
- [ ] 9.2 デフォルトパラメータ（簡略化強度、Chaikin反復回数）の決定理由をコメントまたはドキュメントに残す
